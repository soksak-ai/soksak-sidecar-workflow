{
  "spec": "workflow-doc@0.0.1",
  "meta": {
    "name": "draft",
    "description": "DRAFT 워크플로 — 아이디어(directive)를 백로그 덩어리로 구체화. 단 하나의 자기-루프 op 가 매 라운드 **전체 집합**을 산출하고(델타 아님), 시스템이 add/change/remove 를 계산한다. 새로 산출한 전체가 기존 전체와 동일하면 수렴 = 인증. round-1 은 기존 집합이 ∅ 이라 자연히 전체 생성이 된다."
  },
  "args": {
    "directive": {
      "from": [
        "directive",
        "DIRECTIVE",
        "IDEA"
      ],
      "default": ""
    },
    "parentDraftId": {
      "from": [
        "parentDraftId"
      ],
      "default": null
    }
  },
  "values": {
    "COMMON": "SHARED CONCEPTS:\n- A REQUIREMENT = an imperative the result must satisfy (\"the system/plan/novel/work must …\"): concrete and developable/executable — NOT a background fact, NOT a restatement of the directive, NOT a bare umbrella (\"manage/handle/support X\" that names no specific operation or obligation). (Form: not \"X regulations\" but \"the system must DO <Y> to satisfy <X>\".)\n- MAKE-OR-BREAK = its absence would make the result FAIL or be WRONG, not merely less polished. A genuine one is a DECISION two competent practitioners could resolve DIFFERENTLY — NOT a nice-to-have, NOT one methodology's enumerated beat-list, NOT the HOW / implementation-detail of another requirement (that is covered by its parent, not a separate requirement).\n- EXPERT STANCE: read THIS directive as the SENIOR PRACTITIONER of its real domain (a pharmacist / compliance officer for a drug system, a novelist for a novel, an expedition leader for a climb). An expert never stops at the CATEGORY — \"comply with the narcotics law\" / \"protect personal data\" is NOT a requirement; it HIDES the concrete obligations the law compels, each its own make-or-break (\"on a stock discrepancy, file the incident report to the authority within the statutory deadline\"; \"verify the vendor is a licensed wholesaler at registration\"). Name the SPECIFIC trigger / deadline / check whoever builds, writes, or executes it must satisfy — a distinct requirement an expert knows, never an implementation beat. (Likewise outside law: a novel — not \"a satisfying ending\" but the specific turn that earns it; a plan — not \"be safe\" but the specific abort threshold.) A broad \"support / comply with X\" topic HIDES the gap, it does not cover it.\n- THE BACK-SIDE: the requester is NOT a domain expert — they named the visible SURFACE (the easy 80%) and, even in a DETAILED directive, omit the make-or-break BACK-SIDE (the 20% that decides success) a senior practitioner / law / safety requires for the intent to actually work, be legal, be safe (the administrative, legal, financial, safety, contingency/failure-handling, oversight/who-administers substrate). DRAW IT OUT — adversarially ask of THIS intent: who actually OPERATES it, OVERSEES/administers it, PAYS FOR it, is kept SAFE/legal by it, and RECOVERS it when it fails or ends — and what does each REQUIRE that the requester never said? Don't be seduced by a polished, plausible surface: that polish IS the 80% trap. Then use the per-domain SHAPES below — the KIND to hunt; they COMPLEMENT the questions above (a minimal domain hint), never REPLACE them, and are NOT answers (search the real content; apply ONLY what genuinely fits THIS directive, never force a non-applicable category):\n    · SYSTEM → operator/admin console per permission grade & oversight, data model, regulation (the SPECIFIC reporting/incident triggers, deadlines, and qualifications the governing law compels — kinds to hunt, not answers), security boundaries, monitoring, lifecycle/offboarding.\n    · NOVEL → the avenger's corrosion, justice-vs-vengeance, antagonist depth, the delay engine, the payoff, the aftermath, POV/reveal-order, setting/world, reader complicity.\n    · PLAN → go/no-go gates, per-step verification, contingency/rollback, failure modes, legal/safety preconditions (the SPECIFIC approvals, clearances, and qualifications required before an act may proceed — kinds to hunt, not answers), responsibility, exit criteria.\n    · EVERYDAY (e.g. moving house) → registration, deposit/fee settlement, address changes, defect-check — not just the visible act.\n- THE ACTORS: a directive implies every actor the result touches, yet the requester writes as only one of them. The actors they never played still carry make-or-breaks. Every built thing has, without exception, both on-stage actors, who use the result, and off-stage actors, who run it — the parties that operate, moderate, deploy, and answer for it. The off-stage actors are part of the system itself, never an afterthought. In a web or SaaS service, the off-stage actor is the platform operator, who suspends violating tenants and enforces subscription entitlements. In a game, it is live-ops and moderation, who tune the economy, handle bans and appeals, and roll back incidents. In a desktop or mobile app, it is the deployer, who owns the update channel, triages crashes, and enforces licenses. In an internal system, it is the administrator, who provisions accounts, keeps the audit trail, and rehearses recovery. A third archetype the on-stage/off-stage split misses: EXTERNAL COUNTERPARTIES, who transact THROUGH the result from outside it — by domain, the vendor or supplier who fulfills what users request, the partner who integrates, the external reviewer who signs off — a SEPARATE PARTY with their own surface, not a permission grade of the users' app, their make-or-breaks as load-bearing as any user's. And every distinct actor the directive implies reaches the result through its OWN surface — draw those surfaces as requirements (OPERATING SURFACES below), not only the scattered capabilities behind them. Example requirements from an off-stage actor: \"the operator must be able to suspend a violating tenant and revoke its active sessions\"; \"when a subscription expires, the system must downgrade that tenant's entitlements automatically — the system enforces this, it is not left to good faith.\"\n- OPERATING SURFACES: capabilities are not yet a usable system — walk the OPERATING reality and draw the surfaces the result is actually RUN through, each its own requirement. A capability reachable only through a back-end service, with no surface any actor operates it through, is a MISSING requirement — the surface IS the requirement, not its implementation. Walk them by kind: how each actor is VERIFIABLY authenticated and bound to its role/grade for the session (authentication) — the foundation the whole permission model rests on: without a forgery-proof identity-to-role basis, every permission, access rule, and per-grade console is unenforceable and any caller can claim any role, so wherever roles/grades/permissions exist, authentication is a make-or-break, not an afterthought, how requests reach the capabilities at all (entrypoint / routing), the console each distinct actor and each permission-grade operates through (a per-grade admin console is its own deliverable, NOT implied by the permission model — \"can do X\" is not \"the screen where X is done\"), the screens that read the ledgers / evidence / audit records, what must run on a schedule (jobs / batch), and how the pieces boot and wire together. The test: if the assembled result could be BUILT but not OPERATED — no actor could actually DO the thing — a surface is missing, and that surface is the requirement. Necessity, not mention: draw only surfaces the result cannot be operated without; never pad with polish.\n- LEGAL LENS: wherever the intent's success turns on real-world LAW, RULE, or LEGITIMACY — to be COMPLIANT (a statutory duty it must satisfy), to be PERMITTED (an approval / license / qualification that gates an act or a participation), or to be ACCURATE (a work that portrays or relies on real law) — surface the binding obligations, approvals, triggers, and deadlines the real, current law actually compels, not just the functional surface (ground them by GROUNDING below). This is NOT only for regulated systems: a plan may need a clearance, a novel may need its law right. Apply ONLY where the intent genuinely turns on law; never force it onto one that does not.\n- GROUNDING (when to SEARCH vs REASON — the one rule for any fact you rely on): the real test is \"could you be WRONG from memory?\" — info beyond your knowledge cutoff (a recent event, the CURRENT status of a law/program/standard), OR the SPECIFICS of a named statute/article/standard/figure/framework you could misremember, OR genuine uncertainty → search the web (put the current year in queries; NEVER assert such specifics from memory). When an item rests on what you searched, CITE the grounded source in the item itself — the specific statute/article/standard/version with its identifier or link; a searched item that drops its source reads as an ungrounded guess and cannot be audited. A general principle or common design/craft choice you RELIABLY know → reason it; do NOT search what you reliably know (wasteful), and never re-search the settled.\n- COMMON-SENSE DEFAULT: where the directive leaves a gap or ambiguity, resolve it with the SIMPLEST answer common reasoning reaches — what most competent people would call obvious. Do NOT invent an unusual, elaborate, or restrictive mechanism (a quantity cap, an enforcement, a control) the directive never asked for; a plain reading beats a clever one. The unusual must come from the directive — never from you.\n- INVARIANTS — every requirement, whether GENERATED or ADDED: (1) ATOMIC — one subject, not bundled, not over-split; (2) NO DUPLICATE — not a restatement of another, judged by MEANING not wording (a narrower / re-angled / renamed / split version of an existing one is NOT new); (3) NO FORCING/FABRICATION — a genuine grounded make-or-break, never invented to seem thorough.",
    "SPEC_SET_SCHEMA": {
      "type": "object",
      "required": [
        "requirements",
        "removed"
      ],
      "properties": {
        "requirements": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "title",
              "description"
            ],
            "properties": {
              "id": {
                "type": "string",
                "description": "기존 항목이면 문서의 id 를 그대로. 신규면 비워라 — 없는 id 를 지어내면 계보가 위조된다."
              },
              "title": {
                "type": "string"
              },
              "description": {
                "type": "string"
              },
              "origin": {
                "type": "string",
                "enum": [
                  "user",
                  "agent"
                ]
              },
              "reason": {
                "type": "string",
                "description": "신규 추가·문구 개정·재추가일 때 필수. 원문 그대로 유지하는 항목만 생략한다(추가 시점 근거를 id 로 승계)."
              }
            }
          }
        },
        "removed": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "id",
              "reason"
            ],
            "properties": {
              "id": {
                "type": "string"
              },
              "reason": {
                "type": "string"
              }
            }
          }
        },
        "chunkTitle": {
          "type": "string"
        }
      }
    }
  },
  "prompts": {
    "draft-review": "{{COMMON}}\n\nYOUR ROLE — THE DRAFT LOOP'S SINGLE OPERATION (round {{round}} of max 20). You are not editing a document; you are **producing the complete requirement set as it should stand right now**. Read the whole current set with its history, then emit the FULL set you believe is correct — every requirement that belongs, not just what you would change. The system computes the delta by comparing your set to the previous one, so you never describe changes; you describe the SET.\n\nTHE CURRENT SET — each item with its id, state (o=in the set, x=removed but retained), and full decision history (every add / change / remove with its 근거, in order):\n{{document}}\n\nDirective: \"{{directive}}\"\n\nOUTPUT — `requirements[]` (the complete set) and `removed[]` (what you deliberately take out):\n- **Keep** an existing requirement by listing it with **its exact id and its text unchanged**. Do not re-word what you are not deliberately revising — a needless rewrite is churn, and its 근거 already lives in the item's history, carried forward by id.\n- **Add** a requirement by listing it with **no id** and a `reason` — the 근거 that justifies it, plus `origin` (`\"user\"` if the directive states or clearly implies it, `\"agent\"` if you derived it as a make-or-break the directive never stated).\n- **Change** a requirement by listing it with **its id and revised text plus a `reason`**. Revising is a first-class, welcome operation — a spec is not frozen once written; if a sentence is ambiguous, mis-scoped, or has the wrong context, FIX IT and say why. The id and its whole history are preserved, so the record shows how the wording arrived where it did. But revise only when meaning or clarity genuinely improves: **a rewrite you cannot justify is not a revision, it is noise** — and worse, if you re-word the same idea every round the set never stabilizes and this loop can never converge.\n- **Remove** a requirement by putting it in `removed[]` with its id and a `reason`. It is not deleted — it stays on the board as `x` with its history, so \"why it went\" is never lost.\n- **Re-add** something previously removed by listing it in `requirements[]` with its id and a `reason` that **directly refutes the removal reason recorded in its history**. Its trail becomes add → remove → add. Re-adding on grounds already answered is oscillation — do not.\n\n**EVERY EXISTING ITEM MUST BE ACCOUNTED FOR — kept, changed, or in `removed[]`.** An item you simply omit is not a removal; it is a requirement vanishing with nobody having decided anything, and the system rejects the whole round for it. Silence is not a decision.\n\n**근거 IS THE VERIFICATION.** There is no separate per-item verification pass and no audit stage in this design: the reason you attach when adding, changing, or removing IS that item's verification record, and it is appended to its history permanently. An item added without a real ground is indistinguishable from a fabrication. So every add, change, and remove carries its 근거 — including every single item of the first round, where the set is built from nothing and therefore everything is an add. There is no exemption for the opening round.\n\n**WHEN THE CURRENT SET IS EMPTY, THIS ROUND PRODUCES THE ENTIRE SPEC.** Nothing exists yet, so the set you emit is the whole requirement set for this directive — not a seed, not a skeleton, not the obvious few. **GENERATION IS GENEROUS — cast WIDE.** Include EVERY plausible make-or-break: content, structural/craft, operational, regulated, the back-side, and every actor — on-stage users, external counterparties who transact through it, and above all the off-stage actor who RUNS the result, a part the requester has never played — each drawn through its OWN named surface, not only its scattered functions. The requester CANNOT ask for what they do not know they need — the back-side they never see (crash recovery, security boundaries, AI-execution permission, audit, support operation) is exactly what this pipeline exists to draw out FOR them; it is the product, not padding. AND every DATA ENTITY the result stores, counts, or edits (each noun the directive treats as kept or managed) needs its OWN create / enter / edit / list surface; the CENTRAL entity every other requirement revolves around is the MOST-MISSED requirement of all, dropped because it seems too obvious to state. **No cap, no stinginess; this set must be COMPLETE.** Generosity is SAFE precisely because this same loop prunes — a later round REMOVES whatever does not hold, so over-including costs one remove, while a gap left out may never be found at all. For any real system a handful of requirements is not a set, it is a sample. Also return `chunkTitle` on this round: extract the name the directive states or implies, else generate a short faithful noun phrase in its language.\n\nWrite each requirement as an imperative the result must satisfy; description = one line of what it must do and why it is make-or-break, never a restatement of the title. **INTERPRET, do NOT echo** the directive; a terse clause bundles several DISTINCT requirements — split each into its own atomic item, but never split ONE requirement into implementation beats. **DECIDE, do NOT defer**: FORBIDDEN are 'define whether X or Y', 'determine the policy for Z', 'decide when to …' — where the directive leaves a choice open, MAKE the call and state it as the rule. Sweep the lenses every round: goal-reach · contradiction · seam(경계 소유) · depth(regulated → the SPECIFIC obligation, deadline, trigger) · domain-failure(what breaks in real use) · actor-sweep · operational robustness(concurrency · scale/findability · partial failure · role boundary) · ENTITY-CRUD · CONCRETIZATION (for every distinctive thing the directive spells out — an unusual unit, a special-case rule, a non-default value, an exception — check it is OPERATIONALIZED, not merely named: that value ripples into counting, deduction, display, reconciliation, validation, and those must be present too). Obey the INVARIANTS (atomic · no duplicate by meaning · no fabrication). The GROUNDING branch applies in EVERY round including the first — where its condition fires, search and cite NOW. Do any needed search first.\n\nCONVERGENCE IS STRUCTURAL, NOT A DECLARATION: when the set you produce is identical to the set you were given — nothing added, nothing changed, nothing removed — the loop has converged and that identity IS the certification of this backlog. So emit everything you can justify in THIS round; holding a change back only delays the same work and, if you tire before finishing, certifies a set you never actually completed.\n\nFINAL message = ONLY {\"requirements\":[...],\"removed\":[...]} (plus `chunkTitle` on the first round)."
  },
  "stages": {
    "": [
      {
        "op": "publish",
        "node": {
          "id": "chunk",
          "kind": "chunk",
          "isDraft": true,
          "parentDraftId": {
            "$": "args.parentDraftId",
            "or": ""
          },
          "title": {
            "$": "args.title",
            "or": "구체화 덩어리"
          },
          "description": {
            "$": "args.directive"
          }
        }
      },
      {
        "op": "publish",
        "node": {
          "id": "spec",
          "kind": "section",
          "parent": "chunk",
          "title": "Spec",
          "collapsed": true
        }
      },
      {
        "op": "publish",
        "node": {
          "id": "draft-review",
          "kind": "task",
          "stage": "draft-review",
          "parent": "chunk",
          "title": "완전성 합의 루프"
        }
      }
    ],
    "draft-review": [
      {
        "op": "agent",
        "prompt": "draft-review",
        "schema": "SPEC_SET_SCHEMA",
        "label": "전체집합 산출",
        "bind": "a"
      },
      {
        "op": "publish",
        "node": {
          "id": "draft-review-again",
          "kind": "task",
          "stage": "draft-review",
          "parent": {
            "$": "args.chunkRef",
            "or": "chunk"
          },
          "title": "재검(이견 잔존)",
          "blockedBy": []
        }
      },
      {
        "op": "return",
        "value": {
          "requirements": {
            "$": "a.requirements",
            "or": []
          },
          "removed": {
            "$": "a.removed",
            "or": []
          },
          "chunkTitle": {
            "$": "a.chunkTitle",
            "or": ""
          }
        }
      }
    ]
  }
}
